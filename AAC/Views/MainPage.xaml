<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AAC.Views.MainPage">

    <StackLayout>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Button Text="Группы" Command="{Binding GoToGroupEditingCommand}"/>
            <Button Text="Посещаемость" Command="{Binding GoToAttendanceCommand}"/>
            <Button Text="Настройки" Command="{Binding GoToSettingsCommand}"/>
        </StackLayout>
    </StackLayout>

</ContentPage>
